import DS from 'ember-data';
import Ember from 'ember';

export default DS.JSONSerializer.extend({
	// primaryKey: '_id',

	// modelNameFromPayloadKey(payloadKey) {
	// 	Ember.Logger.info("payloadKey = ", payloadKey)
	//     return this._super(payloadKey);
    // }

});
